<Window x:Class="RacingAidWpf.View.DriversWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:RacingAidWpf.View"
        xmlns:viewModel="clr-namespace:RacingAidWpf.ViewModel"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance viewModel:DriversWindowViewModel}"
        Title="Drivers"
        SizeToContent="WidthAndHeight"
        MaxWidth="800"
        MaxHeight="500"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        Topmost="True"
        AllowsTransparency="True">
    <Window.Background>
        <SolidColorBrush Color="Transparent"/>
    </Window.Background>
    <Grid x:Name="DriversGrid" MouseDown="DriversGrid_OnMouseDown">
        
        <Canvas Opacity="0.3" Background="Black"/>
        
        <DataGrid ItemsSource="{Binding Drivers}"
                  Style="{StaticResource DefaultDataGridStyle}"
                  ColumnHeaderStyle="{StaticResource DefaultDataGridColumnHeaderStyle}"
                  RowStyle="{StaticResource DefaultDataGridRowStyle}"/>
    </Grid>
</Window>
